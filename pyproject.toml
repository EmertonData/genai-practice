[project]
name = "genai-practice"
version = "0.1.0"
description = "GenAI for Finance workshop materials"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "sentence-transformers",
    "rank_bm25",
    "numpy",
    "glide-py",
    "pypdf",
    "jupyterlab",
]

[tool.uv]
package = false
