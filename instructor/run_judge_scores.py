"""Precompute LLM-as-judge faithfulness scores (preparation.md §7.2).

Instructor-only, run once against the shared API key over every pair in
data/paragraph_claims.json. Writes data/judge_scores.json so all students work from
identical proxy labels.
"""


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
