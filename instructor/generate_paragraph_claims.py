"""Generate the paragraph-level faithfulness claim dataset (preparation.md §6.1).

Instructor-only, run once against the shared API key. Writes data/paragraph_claims.json:
(paragraph, claim, true_faithfulness_label, error_type) triples, built from the fixed
hallucination taxonomy, for every chunk of the workshop source document.
"""


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
